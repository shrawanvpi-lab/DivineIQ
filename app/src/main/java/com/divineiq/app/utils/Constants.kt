package com.divineiq.app.utils

/** App-wide constants that don't belong to any single layer. */
object Constants {
    const val DATABASE_NAME = "divineiq.db"
    const val NETWORK_TIMEOUT_SECONDS = 15L
    const val MIN_PASSWORD_LENGTH = 8
}
